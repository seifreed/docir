---
phase: 02-workflow-routing
plan: "04"
type: execute
wave: 3
gap_closure: true
depends_on:
  - "02-03"
files_modified:
  - docs/ci-required-quality-check.md
autonomous: true
requirements:
  - FLOW-04
user_setup:
  - service: github
    why: "FLOW-04 enforcement requires repository admin capability for branch protection/rulesets."
    env_vars:
      - name: GITHUB_TOKEN
        source: "GitHub token with repo admin scope for `gh` CLI/API operations"
must_haves:
  truths:
    - "Canonical CI check `quality-gate` is actively required for merge on the default protected branch/ruleset."
    - "Verification evidence comes from live GitHub API output, not documentation-only intent."
  artifacts:
    - "docs/ci-required-quality-check.md contains dated evidence showing required-check enforcement for `quality-gate`."
  key_links:
    - "Repository branch protection or ruleset configuration includes required status check context `quality-gate`."
---

<objective>
Close the unresolved Phase 2 verification blocker by activating and proving `FLOW-04` enforcement.

Purpose: Convert required-check policy from documented intent to platform-enforced merge protection.
Output: Verified branch protection/ruleset evidence showing `quality-gate` is required for merge.
</objective>

<execution_context>
@/Users/seifreed/.codex/get-shit-done/workflows/execute-plan.md
@/Users/seifreed/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02-workflow-routing/02-VERIFICATION.md
@.planning/phases/02-workflow-routing/02-03-PLAN.md
@.planning/phases/02-workflow-routing/02-03-SUMMARY.md
@docs/ci-required-quality-check.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Validate GitHub capability and target metadata for FLOW-04</name>
  <files>docs/ci-required-quality-check.md</files>
  <action>Run `gh auth status`, resolve repository owner/name and default branch, then query branch protection/ruleset endpoints. Confirm feature gate is unblocked (no `HTTP 403` for required-check configuration APIs) before applying enforcement.</action>
  <verify>
    <automated>gh auth status && gh repo view --json nameWithOwner,defaultBranchRef && gh api repos/seifreed/docir/rulesets</automated>
  </verify>
  <done>Execution has admin-capable access path to apply required-check enforcement for this repository.</done>
</task>

<task type="auto">
  <name>Task 2: Apply required-check enforcement for canonical `quality-gate`</name>
  <files>docs/ci-required-quality-check.md</files>
  <action>Configure repository branch protection or ruleset so merge requires status check context `quality-gate` for the default branch. Use `gh` CLI/API path documented in `docs/ci-required-quality-check.md`; keep check name exact and stable.</action>
  <verify>
    <automated>gh api repos/seifreed/docir/branches/main/protection --include || gh api repos/seifreed/docir/rulesets</automated>
  </verify>
  <done>GitHub protection configuration actively requires `quality-gate` for merge decisions.</done>
</task>

<task type="auto">
  <name>Task 3: Capture objective evidence and close the blocker record</name>
  <files>docs/ci-required-quality-check.md</files>
  <action>Update documentation with date, repository, protected target, commands executed, and command outputs proving required-check contexts include `quality-gate`. Remove or supersede prior blocked-state note for FLOW-04.</action>
  <verify>
    <automated>rg -n "quality-gate|required status checks|ruleset|branch protection|FLOW-04|2026-" docs/ci-required-quality-check.md</automated>
  </verify>
  <done>Evidence is explicit, current, and sufficient for re-verification to mark FLOW-04 as passed.</done>
</task>
</tasks>

<verification>
- [ ] `gh` API inspection shows active required-check configuration that contains `quality-gate` for default-branch merge policy.
- [ ] `docs/ci-required-quality-check.md` includes dated command evidence and no unresolved blocker wording for FLOW-04.
- [ ] Re-running Phase 2 verification can mark `FLOW-04` as Pass.
</verification>

<success_criteria>
- FLOW-04 is no longer blocked and is platform-enforced.
- Evidence is machine-verifiable from API-backed command output.
- Phase 2 has no unresolved verification gap tied to workflow routing.
</success_criteria>

<output>
After completion, create `.planning/phases/02-workflow-routing/02-04-SUMMARY.md`
</output>
