---
phase: 02-workflow-routing
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/quality-gate-policy.md
autonomous: true
requirements:
  - GATE-03
must_haves:
  truths:
    - "Local developer guidance names `./scripts/quality_gate.sh` as the only acceptance command."
    - "Raw cargo commands are explicitly documented as diagnostic-only and non-authoritative for acceptance."
  artifacts:
    - "README.md contains canonical local workflow steps and non-authoritative wording for direct cargo commands."
    - "docs/quality-gate-policy.md includes local workflow routing language aligned to canonical-only acceptance."
  key_links:
    - "README.md local workflow section links policy authority to docs/quality-gate-policy.md."
---

<objective>
Enforce canonical-only local workflow routing in repository documentation.

Purpose: Remove documentation ambiguity that could allow local acceptance through alternate command paths.
Output: Updated local workflow guidance and policy language that only accepts `./scripts/quality_gate.sh` as final quality approval.
</objective>

<execution_context>
@/Users/seifreed/.codex/get-shit-done/workflows/execute-plan.md
@/Users/seifreed/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-workflow-routing/02-CONTEXT.md
@.planning/phases/02-workflow-routing/02-RESEARCH.md
@README.md
@docs/quality-gate-policy.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Normalize README local quality workflow to canonical-only acceptance</name>
  <files>README.md</files>
  <action>Update the local workflow section so final acceptance uses only `./scripts/quality_gate.sh`. Add an explicit README reference/link to `docs/quality-gate-policy.md` as the normative policy authority for routing rules. Keep examples copy-paste friendly from repository root, and preserve any direct `cargo fmt`/`cargo clippy`/`cargo test` references only as fast diagnostic checks, not acceptance authority.</action>
  <verify>
    <automated>rg -n "./scripts/quality_gate.sh|docs/quality-gate-policy.md|non-authoritative|diagnostic" README.md</automated>
  </verify>
  <done>README has one accepted local quality command, no equivalent alternate acceptance wording, and an explicit reference to `docs/quality-gate-policy.md` for normative routing policy.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten policy wording for local workflow routing boundaries</name>
  <files>docs/quality-gate-policy.md</files>
  <action>Update policy text so local development workflow explicitly routes acceptance through canonical gate only, and state that direct raw checks are informational diagnostics. Ensure wording does not imply any alternate accepted local path.</action>
  <verify>
    <automated>rg -n "local|canonical|non-authoritative|forbidden" docs/quality-gate-policy.md</automated>
  </verify>
  <done>Policy and README language are consistent: canonical gate is the only accepted local quality workflow path.</done>
</task>
</tasks>

<verification>
- [ ] `rg -n "./scripts/quality_gate.sh" README.md docs/quality-gate-policy.md` shows canonical command in local workflow guidance.
- [ ] `rg -n "equivalent|or run cargo" README.md docs/quality-gate-policy.md` does not show alternate accepted path language.
- [ ] `bash scripts/tests/quality_gate_contract.sh` still passes after doc updates.
</verification>

<success_criteria>
- GATE-03 is satisfied with canonical-only local workflow documentation.
- Repository docs have no local acceptance ambiguity.
- Canonical policy remains consistent across README and policy file.
</success_criteria>

<output>
After completion, create `.planning/phases/02-workflow-routing/02-01-SUMMARY.md`
</output>
