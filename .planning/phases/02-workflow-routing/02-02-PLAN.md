---
phase: 02-workflow-routing
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - .githooks/pre-commit
  - scripts/install_hooks.sh
  - docs/pre-commit-quality-workflow.md
autonomous: true
requirements:
  - GATE-04
must_haves:
  truths:
    - "Commit-time quality routing executes the canonical gate directly."
    - "Repository-managed hook installation is deterministic and reproducible across clones."
    - "Pre-commit failure behavior reflects canonical gate exit status and final result line."
  artifacts:
    - ".githooks/pre-commit exists and invokes `./scripts/quality_gate.sh` from repository root."
    - "scripts/install_hooks.sh configures `core.hooksPath` to `.githooks` and verifies configured value."
    - "docs/pre-commit-quality-workflow.md documents setup and contract expectations."
  key_links:
    - "scripts/install_hooks.sh configures git to load .githooks/pre-commit."
    - ".githooks/pre-commit resolves repo root then `exec`s canonical gate without duplicating quality logic."
---

<objective>
Route pre-commit quality workflow through the canonical gate using native Git hooks.

Purpose: Prevent local commit acceptance from bypassing canonical quality enforcement.
Output: Versioned pre-commit hook, deterministic install script, and explicit setup/behavior documentation.
</objective>

<execution_context>
@/Users/seifreed/.codex/get-shit-done/workflows/execute-plan.md
@/Users/seifreed/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-workflow-routing/02-CONTEXT.md
@.planning/phases/02-workflow-routing/02-RESEARCH.md
@scripts/quality_gate.sh
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add repository-managed pre-commit hook that delegates to canonical gate</name>
  <files>.githooks/pre-commit</files>
  <action>Create `.githooks/pre-commit` with strict shell mode, resolve repository root using `git rev-parse --show-toplevel`, `cd` to root, and `exec ./scripts/quality_gate.sh`. Do not duplicate raw cargo quality commands in the hook.</action>
  <verify>
    <automated>test -f .githooks/pre-commit && rg -n "git rev-parse --show-toplevel|exec ./scripts/quality_gate.sh" .githooks/pre-commit && bash -n .githooks/pre-commit</automated>
  </verify>
  <done>Tracked pre-commit hook exists and routes only through canonical gate from repository root.</done>
</task>

<task type="auto">
  <name>Task 2: Add deterministic hook installer for `core.hooksPath`</name>
  <files>scripts/install_hooks.sh</files>
  <action>Create installer script that sets `git config core.hooksPath .githooks`, verifies configured value, and emits clear pass/fail output. Keep script purpose strictly to hook path installation; it must not become an alternate quality gate entrypoint.</action>
  <verify>
    <automated>test -f scripts/install_hooks.sh && bash -n scripts/install_hooks.sh && rg -n "core.hooksPath|.githooks" scripts/install_hooks.sh</automated>
  </verify>
  <done>One command can configure deterministic repository-managed hooks with explicit verification output.</done>
</task>

<task type="auto">
  <name>Task 3: Document pre-commit canonical routing setup and behavior contract</name>
  <files>docs/pre-commit-quality-workflow.md</files>
  <action>Document setup steps (`./scripts/install_hooks.sh`), expected `core.hooksPath` value, and commit failure semantics when gate returns non-zero. Explicitly state that final acceptance authority remains canonical gate and that hook output preserves `QUALITY_GATE_RESULT=...` line.</action>
  <verify>
    <automated>rg -n "install_hooks|core.hooksPath|QUALITY_GATE_RESULT|canonical" docs/pre-commit-quality-workflow.md</automated>
  </verify>
  <done>Pre-commit workflow is documented with deterministic setup and canonical-only routing contract.</done>
</task>
</tasks>

<verification>
- [ ] `bash scripts/install_hooks.sh` configures `git config --get core.hooksPath` to `.githooks`.
- [ ] `QUALITY_GATE_FORCE_FAIL=1 ./.githooks/pre-commit` exits non-zero and prints canonical `QUALITY_GATE_RESULT=...` terminal line.
- [ ] `bash scripts/tests/quality_gate_exit_codes.sh` continues to pass.
</verification>

<success_criteria>
- GATE-04 is satisfied by tracked hook routing, deterministic installation, and explicit documentation.
- No pre-commit path duplicates quality checks outside canonical gate.
- Hook behavior propagates canonical pass/fail semantics directly.
</success_criteria>

<output>
After completion, create `.planning/phases/02-workflow-routing/02-02-SUMMARY.md`
</output>

