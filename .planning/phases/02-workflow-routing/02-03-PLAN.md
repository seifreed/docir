---
phase: 02-workflow-routing
plan: "03"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - .github/workflows/quality-gate.yml
  - docs/ci-required-quality-check.md
autonomous: true
requirements:
  - GATE-05
  - FLOW-04
user_setup:
  - service: github
    why: "Ruleset or branch protection update requires repository admin permissions."
    env_vars:
      - name: GITHUB_TOKEN
        source: "GitHub personal access token with repo administration scope used by `gh` CLI"
must_haves:
  truths:
    - "CI quality acceptance executes `./scripts/quality_gate.sh` directly in one stable job."
    - "Canonical CI job name is stable and usable as merge-blocking required check."
    - "Repository protection rules require the canonical CI check for merge."
  artifacts:
    - ".github/workflows/quality-gate.yml defines a single canonical `quality-gate` job."
    - "docs/ci-required-quality-check.md defines required-check configuration and verification steps."
  key_links:
    - "GitHub workflow job `quality-gate` runs only `./scripts/quality_gate.sh` for acceptance."
    - "Ruleset or branch protection required-check context references the stable `quality-gate` job name."
---

<objective>
Route CI merge-blocking quality enforcement through the canonical gate and make it required for merge.

Purpose: Ensure pull request merge eligibility depends on one deterministic canonical quality check.
Output: CI workflow with a single canonical quality job and automation-ready required-check configuration guidance.
</objective>

<execution_context>
@/Users/seifreed/.codex/get-shit-done/workflows/execute-plan.md
@/Users/seifreed/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-workflow-routing/02-CONTEXT.md
@.planning/phases/02-workflow-routing/02-RESEARCH.md
@scripts/quality_gate.sh
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add canonical CI workflow with single stable merge-blocking job</name>
  <files>.github/workflows/quality-gate.yml</files>
  <action>Create GitHub Actions workflow with trigger coverage for pull requests and protected branch pushes. Define one stable job named `quality-gate`; job must run checkout, Rust toolchain setup as needed, and execute only `./scripts/quality_gate.sh` for quality acceptance. Do not add parallel raw quality jobs in this workflow.</action>
  <verify>
    <automated>test -f .github/workflows/quality-gate.yml && rg -n "name: quality-gate|jobs:|quality-gate:|./scripts/quality_gate.sh" .github/workflows/quality-gate.yml</automated>
  </verify>
  <done>Workflow exists with one canonical quality job and no equivalent alternate acceptance jobs.</done>
</task>

<task type="auto">
  <name>Task 2: Add required-check runbook for ruleset/branch-protection mapping</name>
  <files>docs/ci-required-quality-check.md</files>
  <action>Document exact required-check name (`quality-gate`), how to bind it in GitHub rulesets/branch protection, and verification steps proving merges are blocked when the check is missing or failing. Include both UI path and `gh` CLI/API path where available.</action>
  <verify>
    <automated>rg -n "quality-gate|required check|branch protection|ruleset|gh" docs/ci-required-quality-check.md</automated>
  </verify>
  <done>Operational instructions exist to configure and verify `FLOW-04` against the stable canonical job name.</done>
</task>

<task type="auto">
  <name>Task 3: Apply and verify required-check enforcement using GitHub CLI when authorized</name>
  <files>docs/ci-required-quality-check.md</files>
  <action>Use `gh` CLI/API with admin-scoped auth to configure repository ruleset or branch protection so `quality-gate` is required for merge. Record concrete verification evidence in the documentation file: repository, protected branch/ruleset target, and command output showing required status contexts include `quality-gate`. Do not treat blocked-state evidence as completion; this task is complete only when required-check enforcement is actively configured and verified.</action>
  <verify>
    <automated>gh auth status && gh repo view --json nameWithOwner,defaultBranchRef && rg -n "quality-gate.*required|required status checks|enforced" docs/ci-required-quality-check.md</automated>
  </verify>
  <done>`FLOW-04` is configured and evidenced via CLI/API output showing `quality-gate` is an active required check for the target protected branch or ruleset.</done>
</task>
</tasks>

<verification>
- [ ] `.github/workflows/quality-gate.yml` has a single acceptance job named `quality-gate` that executes only `./scripts/quality_gate.sh`.
- [ ] `docs/ci-required-quality-check.md` includes explicit required-check configuration and validation steps.
- [ ] `gh` verification confirms canonical check requirement is actively enforced for merge.
</verification>

<success_criteria>
- GATE-05 is satisfied by direct canonical CI invocation through stable workflow job.
- FLOW-04 is satisfied by required-check configuration evidence tied to `quality-gate`.
- CI quality acceptance surface remains singular and deterministic.
</success_criteria>

<output>
After completion, create `.planning/phases/02-workflow-routing/02-03-SUMMARY.md`
</output>
