---
phase: 04-coverage-integrity-enforcement
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - crates/docir-parser/tests/fixtures.rs
  - crates/docir-cli/tests/coverage_export.rs
  - README.md
  - docs/quality-gate-policy.md
autonomous: true
requirements:
  - TEST-02
must_haves:
  truths:
    - "Coverage-passing tests assert meaningful parser and CLI behavior, not tautological execution-only checks."
    - "Fixture-driven tests validate security-relevant outcomes (nodes/diagnostics/exports) for real sample documents."
    - "Repository policy explicitly forbids synthetic coverage-inflation tests as acceptance evidence."
  artifacts:
    - "crates/docir-parser/tests/fixtures.rs includes behavior-oriented assertions tied to document semantics and diagnostics."
    - "crates/docir-cli/tests/coverage_export.rs includes assertions over coverage/export content semantics, not only command success."
    - "README.md and docs/quality-gate-policy.md define anti-inflation rules and acceptance expectations for coverage tests."
  key_links:
    - "Fixture manifest-backed tests drive parser behavior checks against real fixtures used by canonical test runs."
    - "CLI coverage export tests validate generated data structure/content that coverage stage relies on."
    - "Policy/docs link TEST-02 integrity expectations to canonical gate acceptance rules."
---

<objective>
Establish anti-inflation coverage integrity by strengthening behavior-oriented tests and documenting non-trivial test expectations.

Purpose: Satisfy `TEST-02` so passing coverage represents real confidence in parser/CLI behavior rather than metric gaming.
Output: Enhanced fixture and CLI coverage tests with meaningful assertions plus explicit anti-inflation policy language.
</objective>

<execution_context>
@/Users/seifreed/.codex/get-shit-done/workflows/execute-plan.md
@/Users/seifreed/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-coverage-integrity-enforcement/04-CONTEXT.md
@.planning/phases/04-coverage-integrity-enforcement/04-RESEARCH.md
@.planning/phases/04-coverage-integrity-enforcement/04-01-SUMMARY.md
@crates/docir-parser/tests/fixtures.rs
@crates/docir-cli/tests/coverage_export.rs
@README.md
@docs/quality-gate-policy.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Strengthen parser fixture tests with explicit semantic/security behavior assertions</name>
  <files>crates/docir-parser/tests/fixtures.rs</files>
  <action>Expand fixture assertions to validate concrete behavior outcomes (for example, expected semantic node presence/absence and diagnostic severity/code expectations) for representative rich fixtures. Keep assertions tied to user-visible parser/security behavior and avoid tests that merely execute code paths without validating outputs.</action>
  <verify>
    <automated>cargo test -p docir-parser --test fixtures -- --nocapture</automated>
  </verify>
  <done>Fixture test coverage increases through meaningful parser/security behavior checks rather than synthetic line execution.</done>
</task>

<task type="auto">
  <name>Task 2: Strengthen CLI coverage export tests with content-level contract assertions</name>
  <files>crates/docir-cli/tests/coverage_export.rs</files>
  <action>Extend coverage export integration tests to assert non-trivial data contracts in JSON/CSV outputs (section semantics, status distribution, and expected structural invariants) across real fixtures. Retain command-success checks but make semantic output validation the primary acceptance signal.</action>
  <verify>
    <automated>cargo test -p docir-cli --test coverage_export -- --nocapture</automated>
  </verify>
  <done>CLI coverage tests prove behavior correctness of exported analysis content, not only successful process exit.</done>
</task>

<task type="auto">
  <name>Task 3: Document anti-inflation test policy for canonical acceptance</name>
  <files>README.md, docs/quality-gate-policy.md</files>
  <action>Add concise policy language that forbids tautological or synthetic coverage-inflation tests as acceptance evidence, requires behavior-oriented assertions tied to parser/security outputs, and keeps direct commands diagnostic-only while canonical gate remains authoritative.</action>
  <verify>
    <automated>rg -n "coverage|inflation|synthetic|behavior-oriented|diagnostic-only|canonical" README.md docs/quality-gate-policy.md</automated>
  </verify>
  <done>Documentation provides explicit TEST-02 guardrails and reviewer-visible criteria for meaningful coverage evidence.</done>
</task>
</tasks>

<verification>
- [ ] `cargo test -p docir-parser --test fixtures -- --nocapture` passes with stronger semantic assertions.
- [ ] `cargo test -p docir-cli --test coverage_export -- --nocapture` passes with content-level checks.
- [ ] `cargo llvm-cov --workspace --all-features --summary-only --fail-under-lines 95` passes after integrity-focused tests.
- [ ] `rg -n "coverage|inflation|synthetic|behavior-oriented|diagnostic-only|canonical" README.md docs/quality-gate-policy.md` confirms anti-inflation policy text.
</verification>

<success_criteria>
- `TEST-02` has concrete, behavior-based automated evidence in parser and CLI test suites.
- Coverage improvements are tied to real document-analysis outcomes and not trivial assertions.
- Policy/docs define enforceable anti-inflation expectations aligned with canonical gate semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/04-coverage-integrity-enforcement/04-02-SUMMARY.md`
</output>
