---
phase: 04-coverage-integrity-enforcement
plan: "11"
type: execute
wave: 11
gap_closure: true
depends_on:
  - "04-10"
files_modified:
  - crates/docir-parser/src/odf/spreadsheet.rs
  - crates/docir-parser/src/ooxml/docx/document/inline.rs
  - crates/docir-parser/src/ooxml/xlsx/worksheet.rs
  - crates/docir-parser/src/odf/helpers.rs
  - .planning/phases/04-coverage-integrity-enforcement/04-11-COVERAGE.md
autonomous: true
requirements:
  - CC-04
  - TEST-01
  - TEST-02
must_haves:
  truths:
    - "Canonical `cargo llvm-cov --workspace --all-features --summary-only --fail-under-lines 95` remains the sole quantitative truth for CC-04."
    - "Coverage gains must come from behavior-validating parser assertions (structured outputs/fallback semantics), never execution-only inflation."
    - "Bounded closure contract for 04-11: publish canonical measurements and targeted-module deltas vs 04-10; do not declare Phase 04 closed unless canonical fail-under exits 0."
  artifacts:
    - "Behavior-first residual tests focused on highest-missed 04-10 modules: spreadsheet.rs, inline.rs, worksheet.rs, helpers.rs."
    - "04-11 canonical coverage evidence including fail-under status, workspace total, and targeted module delta table vs 04-10."
    - "Residual handoff shortlist for next increment if fail-under remains non-zero."
  key_links:
    - "04-10-COVERAGE.md is the baseline truth source and residual ranking input for 04-11."
    - "04-VERIFICATION.md requires canonical fail-under pass for phase closure; intermediate increments must preserve this contract."
---

<objective>
Execute the next incremental Phase 04 gap closure after 04-10 by reducing misses in the highest residual parser modules with behavior-first tests and refreshed canonical evidence.

Purpose: continue quantitative movement from the 04-10 canonical baseline (`71.29%`) while preserving strict CC-04/TEST-01/TEST-02 integrity.
Output: scoped hotspot behavior tests plus `.planning/phases/04-coverage-integrity-enforcement/04-11-COVERAGE.md` with canonical truth and residual handoff.
</objective>

<execution_context>
@/Users/seifreed/.codex/get-shit-done/workflows/execute-plan.md
@/Users/seifreed/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/04-coverage-integrity-enforcement/04-VERIFICATION.md
@.planning/phases/04-coverage-integrity-enforcement/04-10-COVERAGE.md
@.planning/phases/04-coverage-integrity-enforcement/04-10-SUMMARY.md
@crates/docir-parser/src/odf/spreadsheet.rs
@crates/docir-parser/src/ooxml/docx/document/inline.rs
@crates/docir-parser/src/ooxml/xlsx/worksheet.rs
@crates/docir-parser/src/odf/helpers.rs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Close highest residual branches in ODF spreadsheet and DOCX inline</name>
  <files>crates/docir-parser/src/odf/spreadsheet.rs, crates/docir-parser/src/ooxml/docx/document/inline.rs</files>
  <action>Add behavior-first tests for uncovered malformed/partial parse paths and fallback branches in `spreadsheet.rs` and `inline.rs`. Assertions must validate parser-visible outcomes (IR fragments, metadata extraction, or fallback classification) rather than line execution.</action>
  <verify>
    <automated>cargo test -p docir-parser odf::spreadsheet --all-features</automated>
    <automated>cargo test -p docir-parser ooxml::docx::document::inline --all-features</automated>
  </verify>
  <done>Top two residual modules receive additional non-trivial behavior assertions targeting currently missed branches from 04-10.</done>
</task>

<task type="auto">
  <name>Task 2: Expand worksheet/helpers behavior coverage for residual fallback semantics</name>
  <files>crates/docir-parser/src/ooxml/xlsx/worksheet.rs, crates/docir-parser/src/odf/helpers.rs</files>
  <action>Add focused tests for worksheet relationship/fallback edge paths and ODF helper coercion/parsing fallbacks still under-covered in 04-10 snapshots. Reject tautological assertions and ensure each test checks structured behavior.</action>
  <verify>
    <automated>cargo test -p docir-parser ooxml::xlsx::worksheet --all-features</automated>
    <automated>cargo test -p docir-parser odf::helpers --all-features</automated>
  </verify>
  <done>Worksheet/helpers residual paths gain meaningful coverage tied to parser behavior, aligned with TEST-02 anti-inflation constraints.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run canonical coverage and publish bounded 04-11 evidence</name>
  <files>.planning/phases/04-coverage-integrity-enforcement/04-11-COVERAGE.md</files>
  <action>Run canonical coverage commands and record: (1) fail-under status, (2) workspace total coverage, (3) delta vs 04-10 baseline (`71.29%`), and (4) targeted module snapshots for `spreadsheet.rs`, `inline.rs`, `worksheet.rs`, and `helpers.rs`. If fail-under remains non-zero, publish a ranked residual shortlist for 04-12.</action>
  <verify>
    <automated>bash scripts/tests/quality_gate_coverage_commands.sh</automated>
    <automated>cargo llvm-cov --workspace --all-features --summary-only --fail-under-lines 95 || true</automated>
    <automated>cargo llvm-cov --workspace --all-features --summary-only</automated>
  </verify>
  <done>04-11 evidence captures canonical truth and a deterministic residual handoff when CC-04 threshold remains unmet.</done>
</task>
</tasks>

<verification>
- [ ] `cargo test -p docir-parser odf::spreadsheet --all-features` passes with behavior assertions for malformed/truncated/fallback outcomes.
- [ ] `cargo test -p docir-parser ooxml::docx::document::inline --all-features` passes with explicit parser-output/fallback assertions.
- [ ] `cargo test -p docir-parser ooxml::xlsx::worksheet --all-features` passes with relationship-resolution fallback behavior checks.
- [ ] `cargo test -p docir-parser odf::helpers --all-features` passes with non-trivial coercion and malformed-input handling assertions.
- [ ] `bash scripts/tests/quality_gate_coverage_commands.sh` passes.
- [ ] `cargo llvm-cov --workspace --all-features --summary-only --fail-under-lines 95` status is recorded as canonical CC-04 truth.
</verification>

<success_criteria>
- 04-11 remains bounded to four residual modules and does not broaden scope beyond CC-04/TEST-01/TEST-02 hotspot closure.
- Canonical truth is preserved: fail-under status is explicitly recorded; phase closure is claimed only on fail-under exit `0`.
- 04-11 publishes deterministic residual handoff data (ranked by missed lines) if threshold is still unmet.
</success_criteria>

<output>
After completion, create `.planning/phases/04-coverage-integrity-enforcement/04-11-SUMMARY.md`
</output>
