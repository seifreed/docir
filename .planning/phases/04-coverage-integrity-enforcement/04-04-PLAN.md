---
phase: 04-coverage-integrity-enforcement
plan: "04"
type: execute
wave: 4
gap_closure: true
depends_on:
  - "04-03"
files_modified:
  - crates/docir-parser/src/parser/security.rs
  - crates/docir-parser/src/parser/metadata.rs
  - crates/docir-security/src/enrich/dde.rs
  - crates/docir-security/src/enrich/xlm.rs
  - crates/docir-security/src/enrich/helpers.rs
  - crates/docir-security/src/enrich.rs
autonomous: true
requirements:
  - CC-04
  - TEST-02
must_haves:
  truths:
    - "Scope targets only remaining CC-04 coverage debt after 04-03; no policy/routing/architecture changes."
    - "Do not rework 04-03-improved modules (`parser/vba.rs`, `parser/analysis.rs`, `security/analyzer.rs`, `serialization/json.rs`) unless a direct dependency blocks testability."
    - "All added tests must validate parser/security semantics (indicator type/level/source/targets), not execution-only inflation."
  artifacts:
    - "Behavior-driven tests covering currently under-tested parser security extraction and metadata parsing branches."
    - "Behavior-driven tests covering DDE/XLM enrichment generation paths including positive and negative cases."
    - "Canonical before/after workspace coverage evidence from `cargo llvm-cov --workspace --all-features --summary-only`."
  key_links:
    - "04-VERIFICATION.md marks Phase 4 incomplete only due to CC-04 threshold shortfall."
    - "04-03-SUMMARY.md shows prior hotspot gains and identifies remaining global coverage debt."
---

<objective>
Close the next highest-impact Phase 4 coverage gap after 04-03 by adding behavior-oriented tests in untouched low-coverage parser/security modules.

Purpose: move workspace coverage materially upward toward `CC-04 >=95%` while preserving `TEST-02` anti-inflation integrity.
Output: focused tests and canonical coverage delta evidence, without reworking already heavily improved 04-03 modules.
</objective>

<execution_context>
@/Users/seifreed/.codex/get-shit-done/workflows/execute-plan.md
@/Users/seifreed/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/04-coverage-integrity-enforcement/04-VERIFICATION.md
@.planning/phases/04-coverage-integrity-enforcement/04-03-SUMMARY.md
@crates/docir-parser/src/parser/security.rs
@crates/docir-parser/src/parser/metadata.rs
@crates/docir-security/src/enrich.rs
@crates/docir-security/src/enrich/dde.rs
@crates/docir-security/src/enrich/xlm.rs
@crates/docir-security/src/enrich/helpers.rs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Cover parser security scanning branches for real extraction outcomes</name>
  <files>crates/docir-parser/src/parser/security.rs</files>
  <action>Add targeted tests for zip-based security scanning paths: macro project detection fallback/positive paths, external relationship typing, ActiveX binary deduplication, and OLE/ExternalReference insertion behavior. Assert concrete IR/security outcomes (node kinds, counts, threat-relevant fields), not only successful execution.</action>
  <verify>
    <automated>cargo test -p docir-parser parser::security --all-features</automated>
  </verify>
  <done>Low-coverage parser security extraction branches execute under behavior-validating assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Cover OOXML metadata parsing edge paths and typed custom-property coercions</name>
  <files>crates/docir-parser/src/parser/metadata.rs</files>
  <action>Add tests that validate core/app/custom property parsing semantics, including absent parts, namespaced tags, integer/float/bool/date/blob coercions, and malformed-value fallbacks. Verify resulting `DocumentMetadata` content rather than parser invocation only.</action>
  <verify>
    <automated>cargo test -p docir-parser parser::metadata --all-features</automated>
  </verify>
  <done>Metadata parser branches gain behavior-level coverage across positive/negative coercion cases.</done>
</task>

<task type="auto">
  <name>Task 3: Cover DDE and XLM enrichment generation paths in security enrichment layer</name>
  <files>crates/docir-security/src/enrich.rs, crates/docir-security/src/enrich/dde.rs, crates/docir-security/src/enrich/xlm.rs, crates/docir-security/src/enrich/helpers.rs</files>
  <action>Add tests for `parse_dde_instruction`, `apply_xlm_defined_name_targets`, `build_xlm_indicators`, remote external reference filtering, and OLE indicator location/description shaping. Validate generated indicator type/level/description/location and mutation semantics for targeted XLM entries.</action>
  <verify>
    <automated>cargo test -p docir-security --all-features</automated>
  </verify>
  <done>Security enrichment hotspots move from low coverage to behavior-validated paths with explicit threat-signal assertions.</done>
</task>

<task type="auto">
  <name>Task 4: Re-measure canonical coverage and record residual gap to CC-04</name>
  <files>crates/docir-parser/src/parser/security.rs, crates/docir-parser/src/parser/metadata.rs, crates/docir-security/src/enrich.rs, crates/docir-security/src/enrich/dde.rs, crates/docir-security/src/enrich/xlm.rs, crates/docir-security/src/enrich/helpers.rs</files>
  <action>Run canonical workspace coverage summary before/after test additions. Record total line coverage delta and whether `--fail-under-lines 95` still fails. If still below threshold, report remaining highest-impact untouched files for next gap closure without revisiting 04-03 modules.</action>
  <verify>
    <automated>cargo llvm-cov --workspace --all-features --summary-only --fail-under-lines 95 || true</automated>
    <automated>cargo llvm-cov --workspace --all-features --summary-only</automated>
  </verify>
  <done>Canonical evidence shows measurable progress toward CC-04 and a precise next residual gap list.</done>
</task>
</tasks>

<verification>
- [ ] `cargo test -p docir-parser parser::security --all-features` passes with behavior assertions on scanner outputs.
- [ ] `cargo test -p docir-parser parser::metadata --all-features` passes with typed property parsing assertions.
- [ ] `cargo test -p docir-security --all-features` passes with DDE/XLM enrichment contract assertions.
- [ ] `cargo llvm-cov --workspace --all-features --summary-only` shows a material increase from the 04-03 post-state (~65.43%).
- [ ] `cargo llvm-cov --workspace --all-features --summary-only --fail-under-lines 95` remains the authoritative CC-04 truth source.
</verification>

<must_haves>
- Gap-only scope: no policy/routing/architecture updates and no broad refactor work.
- Avoid redoing modules already heavily improved in 04-03 unless directly required for dependency seams.
- Tests must prove threat-analysis semantics (classification, location mapping, indicator generation), not synthetic line execution.
</must_haves>

<success_criteria>
- Workspace coverage advances materially beyond the 04-03 result while preserving TEST-02 behavior integrity.
- Newly targeted low-coverage modules (`parser/security`, `parser/metadata`, `security/enrich*`) have concrete behavior-based evidence.
- Residual path to CC-04 is narrowed with an explicit next-gap candidate list if 95% is still not reached.
</success_criteria>

<output>
After completion, create `.planning/phases/04-coverage-integrity-enforcement/04-04-SUMMARY.md`
</output>
