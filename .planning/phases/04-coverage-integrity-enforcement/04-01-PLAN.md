---
phase: 04-coverage-integrity-enforcement
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/quality_gate.sh
  - scripts/tests/quality_gate_baseline_commands.sh
  - scripts/tests/quality_gate_exit_codes.sh
  - scripts/tests/quality_gate_coverage_commands.sh
  - .github/workflows/quality-gate.yml
autonomous: true
requirements:
  - CC-04
  - TEST-01
must_haves:
  truths:
    - "Canonical gate enforces coverage with `cargo llvm-cov` at >=95% and fails deterministically below threshold."
    - "Coverage runs in canonical stage order after fmt/clippy/test and maps failure to `CLASS=quality_failure`."
    - "CI required check `quality-gate` still executes only `./scripts/quality_gate.sh` and cannot bypass coverage."
  artifacts:
    - "scripts/quality_gate.sh contains a centralized coverage threshold constant and a dedicated coverage stage in the canonical dispatcher loop."
    - "scripts/tests/quality_gate_coverage_commands.sh proves llvm-cov invocation contract and below-threshold fail behavior."
    - ".github/workflows/quality-gate.yml installs/ensures coverage tooling and runs canonical gate as the only acceptance command."
  key_links:
    - "run_default_stages in scripts/quality_gate.sh executes `coverage_check` after `test_workspace`."
    - "scripts/tests/quality_gate_exit_codes.sh invokes coverage contract checks in canonical verification flow."
    - "GitHub Actions `quality-gate` job runs `./scripts/quality_gate.sh`, so CI cannot mark success without coverage stage execution."
---

<objective>
Add deterministic coverage-threshold enforcement to the canonical gate and make that enforcement non-skippable in CI.

Purpose: Satisfy `CC-04` and `TEST-01` by turning coverage from policy intent into machine-enforced canonical behavior.
Output: Canonical coverage stage (`cargo llvm-cov` >=95%), deterministic shell contract tests, and CI workflow alignment that preserves canonical-only acceptance.
</objective>

<execution_context>
@/Users/seifreed/.codex/get-shit-done/workflows/execute-plan.md
@/Users/seifreed/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-coverage-integrity-enforcement/04-CONTEXT.md
@.planning/phases/04-coverage-integrity-enforcement/04-RESEARCH.md
@.planning/phases/03-baseline-clean-code-commands/03-01-SUMMARY.md
@scripts/quality_gate.sh
@scripts/tests/quality_gate_baseline_commands.sh
@scripts/tests/quality_gate_exit_codes.sh
@.github/workflows/quality-gate.yml
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add canonical coverage stage with centralized >=95 threshold contract</name>
  <files>scripts/quality_gate.sh</files>
  <action>Add a single source-of-truth threshold constant (`95`) and implement `coverage_check` stage using `cargo llvm-cov` with machine-enforced threshold flags (no custom output parsing). Keep stage order locked as `fmt_check` -> `clippy_strict` -> `test_workspace` -> `coverage_check`, preserve fail-fast behavior, and keep final line contract `QUALITY_GATE_RESULT=... CLASS=... EXIT_CODE=...` with coverage failures classified as quality failures.</action>
  <verify>
    <automated>rg -n "COVERAGE_THRESHOLD|coverage_check|cargo llvm-cov|fail-under-lines|for stage in .*fmt_check.*clippy_strict.*test_workspace.*coverage_check" scripts/quality_gate.sh</automated>
  </verify>
  <done>Canonical gate deterministically enforces coverage >=95% in the only accepted acceptance path.</done>
</task>

<task type="auto">
  <name>Task 2: Add deterministic shell harness for coverage command and threshold-failure behavior</name>
  <files>scripts/tests/quality_gate_coverage_commands.sh, scripts/tests/quality_gate_baseline_commands.sh, scripts/tests/quality_gate_exit_codes.sh</files>
  <action>Create a black-box coverage contract test script (PATH-shim cargo) that asserts llvm-cov command arguments include threshold enforcement and that below-threshold simulated failures produce canonical quality-failure exit semantics. Update existing baseline/exit harnesses so canonical script tests now include coverage stage order and fail-fast checks without introducing alternate gate entrypoints.</action>
  <verify>
    <automated>bash scripts/tests/quality_gate_coverage_commands.sh && bash scripts/tests/quality_gate_baseline_commands.sh && bash scripts/tests/quality_gate_exit_codes.sh</automated>
  </verify>
  <done>Repository has reproducible contract evidence that coverage stage executes and fails correctly when threshold is not met.</done>
</task>

<task type="auto">
  <name>Task 3: Keep CI canonical-only while making llvm-cov tooling deterministic</name>
  <files>.github/workflows/quality-gate.yml</files>
  <action>Update the `quality-gate` workflow to install/ensure `cargo llvm-cov` availability in CI, then continue running only `./scripts/quality_gate.sh` for acceptance. Do not add any alternate required quality job or direct raw cargo coverage command as an acceptance path.</action>
  <verify>
    <automated>rg -n "cargo-llvm-cov|llvm-cov|Run canonical quality gate|\\.\\/scripts\\/quality_gate\\.sh" .github/workflows/quality-gate.yml</automated>
  </verify>
  <done>CI canonical check remains single-path and cannot pass without running the new canonical coverage stage.</done>
</task>
</tasks>

<verification>
- [ ] `bash scripts/tests/quality_gate_contract.sh` passes (canonical entrypoint unchanged).
- [ ] `bash scripts/tests/quality_gate_coverage_commands.sh` passes (coverage invocation + threshold-failure contract).
- [ ] `bash scripts/tests/quality_gate_baseline_commands.sh` and `bash scripts/tests/quality_gate_exit_codes.sh` pass with coverage stage included.
- [ ] `./scripts/quality_gate.sh` executes fmt/clippy/test/coverage in locked order and emits final deterministic result line.
</verification>

<success_criteria>
- `CC-04` is implemented by machine-enforced canonical coverage threshold behavior.
- `TEST-01` is satisfied because CI required check remains canonical-only and therefore cannot skip coverage.
- Coverage enforcement does not create alternate acceptance surfaces.
</success_criteria>

<output>
After completion, create `.planning/phases/04-coverage-integrity-enforcement/04-01-SUMMARY.md`
</output>
