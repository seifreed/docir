---
phase: 04-coverage-integrity-enforcement
plan: "08"
type: execute
wave: 8
gap_closure: true
depends_on:
  - "04-07"
files_modified:
  - crates/docir-parser/src/ooxml/docx/document/inline.rs
  - crates/docir-parser/src/ooxml/xlsx/worksheet.rs
  - crates/docir-parser/src/odf/helpers.rs
  - crates/docir-parser/src/rtf/core.rs
  - crates/docir-parser/src/ooxml/pptx.rs
  - .planning/phases/04-coverage-integrity-enforcement/04-08-COVERAGE.md
autonomous: true
requirements:
  - CC-04
  - TEST-01
  - TEST-02
must_haves:
  truths:
    - "Canonical `cargo llvm-cov --fail-under-lines 95` output remains the sole quantitative acceptance truth for CC-04."
    - "New tests must be behavior-oriented and validate parser outputs/fallback semantics; synthetic execution-only coverage is out of scope."
    - "Coverage evidence must report delta versus 04-07 and publish the next residual shortlist when 95% remains unmet."
  artifacts:
    - "Behavior-first tests for 04-07 residual hotspot modules (inline.rs, worksheet.rs, helpers.rs, core.rs, pptx.rs)."
    - "04-08 canonical coverage evidence file with fail-under result, workspace total, and targeted module snapshots."
    - "Data-driven residual next-step shortlist for 04-09 if CC-04 remains blocked."
  key_links:
    - "04-07-COVERAGE.md defines the residual highest-impact candidates that 04-08 must target incrementally."
    - "04-VERIFICATION.md keeps Phase 04 blocked only on quantitative CC-04 threshold shortfall; TEST-01/TEST-02 remain satisfied."
---

<objective>
Execute the next incremental gap-closure step after 04-07 by expanding behavior-first tests in the current top residual hotspots.

Purpose: increase workspace line coverage from the 04-07 baseline while preserving canonical measurement and anti-inflation integrity constraints.
Output: targeted behavior tests in listed residual modules plus refreshed 04-08 canonical coverage evidence and next shortlist.
</objective>

<execution_context>
@/Users/seifreed/.codex/get-shit-done/workflows/execute-plan.md
@/Users/seifreed/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/04-coverage-integrity-enforcement/04-VERIFICATION.md
@.planning/phases/04-coverage-integrity-enforcement/04-07-COVERAGE.md
@.planning/phases/04-coverage-integrity-enforcement/04-07-SUMMARY.md
@crates/docir-parser/src/ooxml/docx/document/inline.rs
@crates/docir-parser/src/ooxml/xlsx/worksheet.rs
@crates/docir-parser/src/odf/helpers.rs
@crates/docir-parser/src/rtf/core.rs
@crates/docir-parser/src/ooxml/pptx.rs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Expand behavior coverage for OOXML inline and worksheet residual branches</name>
  <files>crates/docir-parser/src/ooxml/docx/document/inline.rs, crates/docir-parser/src/ooxml/xlsx/worksheet.rs</files>
  <action>Add focused parser behavior tests for high-missed branches in DOCX inline and XLSX worksheet modules, including malformed XML/partial structure handling, branch-specific fallback outcomes, and concrete IR/output assertions. Do not add invocation-only or tautological assertions.</action>
  <verify>
    <automated>cargo test -p docir-parser ooxml::docx::document::inline --all-features</automated>
    <automated>cargo test -p docir-parser ooxml::xlsx::worksheet --all-features</automated>
  </verify>
  <done>Inline and worksheet hotspots gain meaningful behavior assertions that exercise under-covered parse and fallback paths.</done>
</task>

<task type="auto">
  <name>Task 2: Add behavior-first tests for ODF helper, RTF core, and OOXML PPTX residual hotspots</name>
  <files>crates/docir-parser/src/odf/helpers.rs, crates/docir-parser/src/rtf/core.rs, crates/docir-parser/src/ooxml/pptx.rs</files>
  <action>Add targeted tests for helper coercion/parsing edge cases, RTF control-flow and malformed-stream recovery, and PPTX parse/fallback branches identified in residual coverage. Assertions must validate structured outputs/classifications and security-relevant behavior, not line execution alone.</action>
  <verify>
    <automated>cargo test -p docir-parser odf::helpers --all-features</automated>
    <automated>cargo test -p docir-parser rtf::core --all-features</automated>
    <automated>cargo test -p docir-parser ooxml::pptx --all-features</automated>
  </verify>
  <done>helpers.rs, core.rs, and pptx.rs receive behavior-oriented branch coverage improvements aligned with TEST-02.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run canonical coverage and publish 04-08 residual inventory</name>
  <files>.planning/phases/04-coverage-integrity-enforcement/04-08-COVERAGE.md</files>
  <action>Run canonical coverage commands and record fail-under status, workspace total line coverage, and delta versus 04-07 baseline (69.78%). Include targeted module snapshots for inline.rs, worksheet.rs, helpers.rs, core.rs, and pptx.rs. If still below 95%, publish next highest-impact residual candidates for 04-09 without widening scope to deferred phases.</action>
  <verify>
    <automated>cargo llvm-cov --workspace --all-features --summary-only --fail-under-lines 95 || true</automated>
    <automated>cargo llvm-cov --workspace --all-features --summary-only</automated>
  </verify>
  <done>04-08 evidence captures canonical truth metrics, quantitative movement from 04-07, and a data-driven follow-on shortlist.</done>
</task>
</tasks>

<verification>
- [ ] `cargo test -p docir-parser ooxml::docx::document::inline --all-features` passes with behavior-oriented assertions.
- [ ] `cargo test -p docir-parser ooxml::xlsx::worksheet --all-features` passes with malformed/partial input fallback assertions.
- [ ] `cargo test -p docir-parser odf::helpers --all-features` passes with helper coercion and fallback behavior checks.
- [ ] `cargo test -p docir-parser rtf::core --all-features` passes with control-flow and malformed-stream recovery assertions.
- [ ] `cargo test -p docir-parser ooxml::pptx --all-features` passes with concrete parse-output and fallback checks.
- [ ] `cargo llvm-cov --workspace --all-features --summary-only` reports measurable movement from 04-07 baseline (69.78%).
- [ ] `cargo llvm-cov --workspace --all-features --summary-only --fail-under-lines 95` remains the authoritative CC-04 acceptance truth.
</verification>

<success_criteria>
- All five residual highest-impact candidates from 04-07 receive behavior-first test expansion with no synthetic inflation patterns.
- Workspace line coverage improves beyond the 04-07 baseline while canonical threshold enforcement semantics remain unchanged.
- Residual path to 95% is narrowed and explicitly documented for the next incremental gap-closure step.
</success_criteria>

<output>
After completion, create `.planning/phases/04-coverage-integrity-enforcement/04-08-SUMMARY.md`
</output>
