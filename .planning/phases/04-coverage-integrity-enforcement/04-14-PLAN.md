---
phase: 04-coverage-integrity-enforcement
plan: "14"
type: execute
wave: 14
gap_closure: true
depends_on:
  - "04-13"
files_modified:
  - crates/docir-diff/src/summary.rs
  - crates/docir-diff/src/summary/presentation.rs
  - crates/docir-diff/src/summary/spreadsheet.rs
  - crates/docir-diff/src/index.rs
  - crates/docir-core/src/visitor/visitors.rs
  - crates/docir-cli/tests/summary_security_commands.rs
  - .planning/phases/04-coverage-integrity-enforcement/04-14-COVERAGE.md
autonomous: true
requirements:
  - CC-04
  - TEST-01
  - TEST-02
must_haves:
  truths:
    - "Canonical `cargo llvm-cov --workspace --all-features --summary-only --fail-under-lines 95` remains the sole quantitative truth for CC-04."
    - "Coverage growth must come from behavior assertions over real command/parser behavior."
  artifacts:
    - "Cross-crate missed-line ranking from llvm-cov JSON summary."
    - "Behavior tests for high-miss docir-diff/docir-core paths and CLI summary/security command paths."
    - "Updated canonical coverage evidence with residual blocker profile."
---

<objective>
Execute an aggressive cross-crate Phase 04 increment driven by current workspace hotspot ranking, then re-measure canonical fail-under truth.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Close high-miss docir-diff and docir-core utility hotspots</name>
  <files>crates/docir-diff/src/summary.rs, crates/docir-diff/src/summary/presentation.rs, crates/docir-diff/src/summary/spreadsheet.rs, crates/docir-diff/src/index.rs, crates/docir-core/src/visitor/visitors.rs</files>
  <action>Add behavior tests for summary/index/visitor branches with deterministic assertions over node summaries, signatures, keying, and counters.</action>
</task>

<task type="auto">
  <name>Task 2: Add behavior tests for CLI summary/security command outputs</name>
  <files>crates/docir-cli/tests/summary_security_commands.rs</files>
  <action>Add integration tests that execute `docir summary` and `docir security` (human/json) on real fixtures and validate output behavior and schema fields.</action>
</task>

<task type="auto">
  <name>Task 3: Re-run canonical coverage and publish 04-14 evidence</name>
  <files>.planning/phases/04-coverage-integrity-enforcement/04-14-COVERAGE.md</files>
  <action>Capture fail-under totals after each increment and publish blocker inventory from full workspace missed-line ranking.</action>
</task>
</tasks>
