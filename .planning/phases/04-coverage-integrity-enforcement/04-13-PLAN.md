---
phase: 04-coverage-integrity-enforcement
plan: "13"
type: execute
wave: 13
gap_closure: true
depends_on:
  - "04-12"
files_modified:
  - crates/docir-parser/src/ooxml/docx/document/inline.rs
  - crates/docir-parser/src/ooxml/xlsx/worksheet.rs
  - crates/docir-parser/src/odf/helpers.rs
  - crates/docir-parser/src/odf/spreadsheet.rs
  - .planning/phases/04-coverage-integrity-enforcement/04-13-COVERAGE.md
autonomous: true
requirements:
  - CC-04
  - TEST-01
  - TEST-02
must_haves:
  truths:
    - "Canonical `cargo llvm-cov --workspace --all-features --summary-only --fail-under-lines 95` remains the sole quantitative truth for CC-04."
    - "Coverage gains must come from behavior-validating assertions over parser/binding outcomes, never execution-only inflation."
    - "Bounded closure contract for 04-13: publish canonical measurements and targeted-module deltas vs 04-12; do not declare Phase 04 closed unless canonical fail-under exits 0."
  artifacts:
    - "Behavior-first tests for highest-missed 04-12 residual modules: inline.rs, worksheet.rs, helpers.rs, spreadsheet.rs."
    - "04-13 canonical coverage evidence including fail-under status, workspace total, and targeted module delta table vs 04-12."
    - "Residual handoff shortlist for next increment if fail-under remains non-zero."
  key_links:
    - "04-12-COVERAGE.md is the baseline truth source and residual ranking input for 04-13."
    - "04-VERIFICATION.md requires canonical fail-under pass for phase closure; intermediate increments must preserve this contract."
---

<objective>
Execute the next incremental Phase 04 gap closure after 04-12 by reducing misses in bounded high-yield residual modules with behavior-first tests and refreshed canonical evidence.

Purpose: continue quantitative movement from the 04-12 canonical baseline (`72.86%`) while preserving strict CC-04/TEST-01/TEST-02 integrity.
Output: scoped hotspot behavior tests plus `.planning/phases/04-coverage-integrity-enforcement/04-13-COVERAGE.md` with canonical truth and residual handoff.
</objective>

<execution_context>
@/Users/seifreed/.codex/get-shit-done/workflows/execute-plan.md
@/Users/seifreed/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/04-coverage-integrity-enforcement/04-VERIFICATION.md
@.planning/phases/04-coverage-integrity-enforcement/04-12-COVERAGE.md
@.planning/phases/04-coverage-integrity-enforcement/04-12-SUMMARY.md
@crates/docir-parser/src/ooxml/docx/document/inline.rs
@crates/docir-parser/src/ooxml/xlsx/worksheet.rs
@crates/docir-parser/src/odf/helpers.rs
@crates/docir-parser/src/odf/spreadsheet.rs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Expand inline and worksheet behavior coverage for top residual branches</name>
  <files>crates/docir-parser/src/ooxml/docx/document/inline.rs, crates/docir-parser/src/ooxml/xlsx/worksheet.rs</files>
  <action>Add behavior-first tests for malformed/truncated/fallback branches still missed in `inline.rs` and `worksheet.rs`. Assertions must validate parser-visible outputs and fallback outcomes, not execution-only line hits.</action>
  <verify>
    <automated>cargo test -p docir-parser ooxml::docx::document::inline --all-features</automated>
    <automated>cargo test -p docir-parser ooxml::xlsx::worksheet --all-features</automated>
  </verify>
  <done>Top residual modules receive additional non-trivial assertions for currently missed branch families from 04-12.</done>
</task>

<task type="auto">
  <name>Task 2: Expand helpers and spreadsheet fallback/coercion coverage</name>
  <files>crates/docir-parser/src/odf/helpers.rs, crates/docir-parser/src/odf/spreadsheet.rs</files>
  <action>Add focused tests for unresolved helper coercion/malformed-input paths and spreadsheet fallback/edge-case branches still under-covered in 04-12 snapshots. Reject tautological assertions.</action>
  <verify>
    <automated>cargo test -p docir-parser odf::helpers --all-features</automated>
    <automated>cargo test -p docir-parser odf::spreadsheet --all-features</automated>
  </verify>
  <done>ODF helper/spreadsheet residual paths gain meaningful behavior coverage tied to parser outcomes, aligned with TEST-02 anti-inflation constraints.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run canonical coverage and publish bounded 04-13 evidence</name>
  <files>.planning/phases/04-coverage-integrity-enforcement/04-13-COVERAGE.md</files>
  <action>Run canonical coverage commands and record: (1) fail-under status, (2) workspace total coverage, (3) delta vs 04-12 baseline (`72.86%`), and (4) targeted module snapshots for `inline.rs`, `worksheet.rs`, `helpers.rs`, and `spreadsheet.rs`. If fail-under remains non-zero, publish a ranked residual shortlist for 04-14.</action>
  <verify>
    <automated>bash scripts/tests/quality_gate_coverage_commands.sh</automated>
    <automated>cargo llvm-cov --workspace --all-features --summary-only --fail-under-lines 95 || true</automated>
    <automated>cargo llvm-cov --workspace --all-features --summary-only</automated>
  </verify>
  <done>04-13 evidence captures canonical truth and a deterministic residual handoff when CC-04 threshold remains unmet.</done>
</task>
</tasks>

<verification>
- [ ] `cargo test -p docir-parser ooxml::docx::document::inline --all-features` passes with behavior assertions for malformed/truncated/fallback outcomes.
- [ ] `cargo test -p docir-parser ooxml::xlsx::worksheet --all-features` passes with relationship-resolution fallback behavior checks.
- [ ] `cargo test -p docir-parser odf::helpers --all-features` passes with non-trivial coercion and malformed-input handling assertions.
- [ ] `cargo test -p docir-parser odf::spreadsheet --all-features` passes with behavior assertions for spreadsheet edge-case parsing paths.
- [ ] `bash scripts/tests/quality_gate_coverage_commands.sh` passes.
- [ ] `cargo llvm-cov --workspace --all-features --summary-only --fail-under-lines 95` status is recorded as canonical CC-04 truth.
</verification>

<success_criteria>
- 04-13 remains bounded to explicit residual modules and does not broaden scope beyond CC-04/TEST-01/TEST-02 hotspot closure.
- Canonical truth is preserved: fail-under status is explicitly recorded; phase closure is claimed only on fail-under exit `0`.
- 04-13 publishes deterministic residual handoff data (ranked by missed lines) if threshold is still unmet.
</success_criteria>

<output>
After completion, create `.planning/phases/04-coverage-integrity-enforcement/04-13-SUMMARY.md`
</output>
