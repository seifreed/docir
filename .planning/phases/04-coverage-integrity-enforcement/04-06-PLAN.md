---
phase: 04-coverage-integrity-enforcement
plan: "06"
type: execute
wave: 6
gap_closure: true
depends_on:
  - "04-05"
files_modified:
  - crates/docir-parser/src/ooxml/docx/document/inline.rs
  - crates/docir-parser/src/ooxml/xlsx/worksheet.rs
  - crates/docir-parser/src/rtf/core.rs
  - .planning/phases/04-coverage-integrity-enforcement/04-06-COVERAGE.md
autonomous: true
requirements:
  - CC-04
  - TEST-01
  - TEST-02
must_haves:
  truths:
    - "Canonical fail-under-95 coverage command remains the only CC-04 truth source for acceptance."
    - "New tests in OOXML and RTF hotspots assert parser behavior and security-relevant outcomes, not synthetic execution-only paths."
    - "Post-run evidence records measurable coverage movement and updated residual blockers when 95% is not yet reached."
  artifacts:
    - "Behavior-oriented tests in docx inline parser, xlsx worksheet parser, and rtf core parser modules."
    - "Coverage evidence file for 04-06 containing canonical command outputs and residual hotspot inventory."
    - "Updated per-file hotspot snapshot for modules targeted in 04-06."
  key_links:
    - "04-05-COVERAGE.md identifies inline.rs, worksheet.rs, and rtf/core.rs as highest-impact untouched candidates."
    - "04-VERIFICATION.md keeps Phase 4 blocked only on CC-04 quantitative threshold shortfall."
---

<objective>
Close the next highest-impact residual coverage gap after 04-05 by targeting top untouched OOXML/RTF hotspot modules with behavior-first tests.

Purpose: increase workspace line coverage toward >=95% while preserving TEST-02 anti-inflation integrity and canonical TEST-01 measurement semantics.
Output: focused hotspot tests plus refreshed canonical coverage evidence and residual shortlist.
</objective>

<execution_context>
@/Users/seifreed/.codex/get-shit-done/workflows/execute-plan.md
@/Users/seifreed/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/04-coverage-integrity-enforcement/04-VERIFICATION.md
@.planning/phases/04-coverage-integrity-enforcement/04-05-COVERAGE.md
@.planning/phases/04-coverage-integrity-enforcement/04-05-SUMMARY.md
@crates/docir-parser/src/ooxml/docx/document/inline.rs
@crates/docir-parser/src/ooxml/xlsx/worksheet.rs
@crates/docir-parser/src/rtf/core.rs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add behavior-oriented coverage for OOXML inline and worksheet parsing branches</name>
  <files>crates/docir-parser/src/ooxml/docx/document/inline.rs, crates/docir-parser/src/ooxml/xlsx/worksheet.rs</files>
  <action>Add targeted tests for inline object extraction and worksheet parsing edge paths, including malformed/partial XML handling, structured output shaping, and branch-specific fallback behavior. Assertions must validate parsed IR/results and branch outcomes rather than call execution only.</action>
  <verify>
    <automated>cargo test -p docir-parser ooxml::docx::document::inline --all-features</automated>
    <automated>cargo test -p docir-parser ooxml::xlsx::worksheet --all-features</automated>
  </verify>
  <done>OOXML hotspot branches are covered with concrete behavior assertions for positive and negative parser outcomes.</done>
</task>

<task type="auto">
  <name>Task 2: Add behavior-oriented RTF core parser tests for control-flow and fallback semantics</name>
  <files>crates/docir-parser/src/rtf/core.rs</files>
  <action>Add focused tests for RTF core parsing paths that currently miss lines, including token/control-word handling, nested group behavior, malformed stream recovery, and security-relevant marker outcomes. Verify emitted structures/classifications and fallback semantics explicitly.</action>
  <verify>
    <automated>cargo test -p docir-parser rtf::core --all-features</automated>
  </verify>
  <done>RTF core low-coverage branches are exercised via behavior-validating assertions instead of synthetic inflation tests.</done>
</task>

<task type="auto">
  <name>Task 3: Re-measure canonical workspace coverage and publish 04-06 evidence</name>
  <files>.planning/phases/04-coverage-integrity-enforcement/04-06-COVERAGE.md</files>
  <action>Run canonical coverage commands, record fail-under status and total line coverage delta versus 04-05 (68.10%), and capture targeted-module snapshots for inline.rs, worksheet.rs, and rtf/core.rs. If still below 95%, list next residual highest-impact candidates for 04-07 without altering policy/routing/architecture scope.</action>
  <verify>
    <automated>cargo llvm-cov --workspace --all-features --summary-only --fail-under-lines 95 || true</automated>
    <automated>cargo llvm-cov --workspace --all-features --summary-only</automated>
  </verify>
  <done>Canonical evidence is updated with quantitative progress and an explicit residual shortlist for the next gap-closure step.</done>
</task>
</tasks>

<verification>
- [ ] `cargo test -p docir-parser ooxml::docx::document::inline --all-features` passes with behavior assertions.
- [ ] `cargo test -p docir-parser ooxml::xlsx::worksheet --all-features` passes with behavior assertions.
- [ ] `cargo test -p docir-parser rtf::core --all-features` passes with parser outcome and fallback assertions.
- [ ] `cargo llvm-cov --workspace --all-features --summary-only` reports measurable increase over the 04-05 total (68.10%).
- [ ] `cargo llvm-cov --workspace --all-features --summary-only --fail-under-lines 95` remains canonical acceptance truth for CC-04.
</verification>

<success_criteria>
- Highest-impact untouched OOXML/RTF hotspots from 04-05 receive behavior-first test expansion with no synthetic coverage patterns.
- Workspace line coverage increases beyond the 04-05 baseline while preserving canonical measurement constraints.
- Residual path to 95% is narrowed and explicitly documented for the next gap-closure plan.
</success_criteria>

<output>
After completion, create `.planning/phases/04-coverage-integrity-enforcement/04-06-SUMMARY.md`
</output>
