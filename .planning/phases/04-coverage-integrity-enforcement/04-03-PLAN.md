---
phase: 04-coverage-integrity-enforcement
plan: "03"
type: execute
wave: 3
gap_closure: true
depends_on:
  - "04-02"
files_modified:
  - crates/docir-parser/src/parser/vba.rs
  - crates/docir-parser/src/parser/analysis.rs
  - crates/docir-security/src/analyzer.rs
  - crates/docir-serialization/src/json.rs
autonomous: true
requirements:
  - CC-04
  - TEST-02
must_haves:
  truths:
    - "Scope addresses only the unresolved Phase 4 gap: workspace coverage threshold shortfall (~63.19% vs >=95%)."
    - "Added tests are behavior-oriented and cover real parser/security/serialization outcomes, not tautological inflation checks."
    - "Coverage improvement is demonstrated from the same canonical measurement command used by the gate."
  artifacts:
    - "New unit/integration tests in parser/security/serialization hotspots with currently low coverage."
    - "Before/after `cargo llvm-cov --workspace --all-features --summary-only` evidence showing a material increase from the ~63.19% baseline."
  key_links:
    - "Phase 4 verification blocker in 04-VERIFICATION.md identifies coverage threshold shortfall as the only unresolved gap."
    - "Canonical enforcement remains in scripts/quality_gate.sh; this plan only reduces coverage debt to satisfy CC-04."
---

<objective>
Close the remaining Phase 4 blocker by materially increasing measured workspace line coverage from the current ~63.19% baseline toward the enforced >=95% threshold.

Purpose: Address only `CC-04` gap debt while preserving `TEST-02` anti-inflation integrity.
Output: New behavior-driven tests in high-impact low-coverage modules plus reproducible coverage-delta evidence from canonical `cargo llvm-cov` measurement.
</objective>

<execution_context>
@/Users/seifreed/.codex/get-shit-done/workflows/execute-plan.md
@/Users/seifreed/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/04-coverage-integrity-enforcement/04-VERIFICATION.md
@.planning/phases/04-coverage-integrity-enforcement/04-02-SUMMARY.md
@crates/docir-parser/src/parser/vba.rs
@crates/docir-parser/src/parser/analysis.rs
@crates/docir-security/src/analyzer.rs
@crates/docir-serialization/src/json.rs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add focused parser helper tests in low-coverage hotspots</name>
  <files>crates/docir-parser/src/parser/vba.rs, crates/docir-parser/src/parser/analysis.rs</files>
  <action>Add `#[cfg(test)]` suites that validate real behavior for VBA project parsing/decompression and parser analysis helpers (ActiveX XML extraction, SmartArt parsing, chart-series extraction, and error mapping). Prioritize branch coverage over fixture volume and avoid assertion-only smoke tests.</action>
  <verify>
    <automated>cargo test -p docir-parser --all-features</automated>
  </verify>
  <done>Parser helper hotspots gain meaningful behavioral coverage across positive and negative branches.</done>
</task>

<task type="auto">
  <name>Task 2: Add security analyzer behavior tests for threat synthesis paths</name>
  <files>crates/docir-security/src/analyzer.rs</files>
  <action>Add tests that build minimal IR stores with document/security nodes and verify analyzer output: threat level escalation, finding generation across macro/OLE/external/ActiveX paths, boolean feature flags, and report formatting contracts.</action>
  <verify>
    <automated>cargo test -p docir-security --all-features</automated>
  </verify>
  <done>Security analyzer code paths currently at 0% are executed through behavior-validating assertions.</done>
</task>

<task type="auto">
  <name>Task 3: Add serialization contract tests for deterministic JSON output</name>
  <files>crates/docir-serialization/src/json.rs</files>
  <action>Add tests for tree/store serialization contracts covering pretty vs compact output, deterministic key ordering, include/exclude span behavior, and not-found root error handling.</action>
  <verify>
    <automated>cargo test -p docir-serialization --all-features</automated>
  </verify>
  <done>Serialization module currently at 0% gains direct behavior coverage with deterministic output assertions.</done>
</task>

<task type="auto">
  <name>Task 4: Re-measure workspace coverage and record delta against blocker baseline</name>
  <files>crates/docir-parser/src/parser/vba.rs, crates/docir-parser/src/parser/analysis.rs, crates/docir-security/src/analyzer.rs, crates/docir-serialization/src/json.rs</files>
  <action>Run canonical coverage measurement after new tests and compare total workspace line coverage to the blocker baseline (~63.19%). If still below 95%, keep scope to the same modules and iterate test depth until a material increase is observed without synthetic assertions.</action>
  <verify>
    <automated>cargo llvm-cov --workspace --all-features --summary-only --fail-under-lines 95 || true</automated>
    <automated>cargo llvm-cov --workspace --all-features --summary-only</automated>
  </verify>
  <done>Coverage delta evidence shows measurable progress toward CC-04 from the same canonical metric source.</done>
</task>
</tasks>

<verification>
- [ ] `cargo test -p docir-parser --all-features` passes with new parser helper behavior tests.
- [ ] `cargo test -p docir-security --all-features` passes with analyzer behavior tests.
- [ ] `cargo test -p docir-serialization --all-features` passes with JSON contract tests.
- [ ] `cargo llvm-cov --workspace --all-features --summary-only` reports a material increase above the ~63.19% blocker baseline.
- [ ] `cargo llvm-cov --workspace --all-features --summary-only --fail-under-lines 95` remains the acceptance truth source for CC-04 closure.
</verification>

<must_haves>
- Coverage work remains confined to unresolved Phase 4 gap debt (no policy/routing/architecture scope changes).
- Test additions validate semantics and security behavior, not synthetic inflation patterns.
- Verification includes explicit before/after percentage evidence tied to canonical llvm-cov measurement.
</must_haves>

<success_criteria>
- The only unresolved Phase 4 verification gap (CC-04 threshold shortfall) has a concrete, minimal execution path.
- Workspace coverage moves materially upward from ~63.19% through behavior-oriented tests.
- Test integrity posture from `TEST-02` is preserved while raising coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/04-coverage-integrity-enforcement/04-03-SUMMARY.md`
</output>
