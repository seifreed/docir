---
phase: 04-coverage-integrity-enforcement
plan: "10"
type: execute
wave: 10
gap_closure: true
depends_on:
  - "04-09"
files_modified:
  - crates/docir-parser/src/odf/spreadsheet.rs
  - crates/docir-parser/src/ooxml/docx/document/inline.rs
  - crates/docir-parser/src/ooxml/xlsx/worksheet.rs
  - crates/docir-parser/src/odf/presentation_helpers.rs
  - crates/docir-parser/src/odf/helpers.rs
  - .planning/phases/04-coverage-integrity-enforcement/04-10-COVERAGE.md
autonomous: true
requirements:
  - CC-04
  - TEST-01
  - TEST-02
must_haves:
  truths:
    - "Plan 04-10 acceptance is blocked until `cargo llvm-cov --workspace --all-features --summary-only --fail-under-lines 95` exits 0."
    - "Coverage growth must come from behavior-validating parser assertions, not execution-only inflation."
    - "Canonical fail-under output remains the single quantitative acceptance truth for CC-04 and completion acceptance."
  artifacts:
    - "Behavior-first test additions for highest-missed modules from 04-09 residual evidence."
    - "04-10 canonical coverage evidence file with passing fail-under result, workspace total, and module deltas vs 04-09."
  key_links:
    - "04-09-COVERAGE.md residual missed-line ranking drives 04-10 scope ordering."
    - "04-10 completion acceptance is valid only after canonical fail-under command passes."
---

<objective>
Close the remaining residual coverage gap by targeting the highest missed-line modules from 04-09 with behavior-first tests and refreshed canonical evidence.

Purpose: reduce the 24.09-point remaining threshold delta while preserving strict TEST-02 integrity and canonical CC-04 truth semantics.
Output: targeted parser behavior tests plus 04-10 canonical evidence proving threshold closure via canonical fail-under pass.
</objective>

<execution_context>
@/Users/seifreed/.codex/get-shit-done/workflows/execute-plan.md
@/Users/seifreed/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/04-coverage-integrity-enforcement/04-VERIFICATION.md
@.planning/phases/04-coverage-integrity-enforcement/04-09-COVERAGE.md
@.planning/phases/04-coverage-integrity-enforcement/04-09-SUMMARY.md
@crates/docir-parser/src/odf/spreadsheet.rs
@crates/docir-parser/src/ooxml/docx/document/inline.rs
@crates/docir-parser/src/ooxml/xlsx/worksheet.rs
@crates/docir-parser/src/odf/presentation_helpers.rs
@crates/docir-parser/src/odf/helpers.rs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Expand behavior tests in top two residual hotspots (ODF spreadsheet + DOCX inline)</name>
  <files>crates/docir-parser/src/odf/spreadsheet.rs, crates/docir-parser/src/ooxml/docx/document/inline.rs</files>
  <action>Add focused tests for currently under-covered branches with malformed/partial input paths and explicit IR/fallback assertions. Prioritize branch families with the highest missed-line counts from 04-09 (`spreadsheet.rs`, `inline.rs`) and reject tautological assertions that only execute lines without validating outcomes.</action>
  <verify>
    <automated>cargo test -p docir-parser odf::spreadsheet --all-features</automated>
    <automated>cargo test -p docir-parser ooxml::docx::document::inline --all-features</automated>
  </verify>
  <done>Both hotspot modules gain measurable behavior-oriented branch coverage aligned with TEST-02 and current residual ranking.</done>
</task>

<task type="auto">
  <name>Task 2: Expand residual behavior coverage for worksheet and ODF helper modules</name>
  <files>crates/docir-parser/src/ooxml/xlsx/worksheet.rs, crates/docir-parser/src/odf/presentation_helpers.rs, crates/docir-parser/src/odf/helpers.rs</files>
  <action>Add branch-focused tests for worksheet relationship/fallback handling and ODF helper edge classification paths (presentation helpers + shared helpers). Assertions must validate structured outputs, fallback classification, and malformed-input semantics; avoid execution-only inflation patterns.</action>
  <verify>
    <automated>cargo test -p docir-parser ooxml::xlsx::worksheet --all-features</automated>
    <automated>cargo test -p docir-parser odf::presentation_helpers --all-features</automated>
    <automated>cargo test -p docir-parser odf::helpers --all-features</automated>
  </verify>
  <done>Worksheet/presentation_helpers/helpers receive behavior-first residual closure aligned with anti-inflation policy.</done>
</task>

<task type="auto">
  <name>Task 3: Re-measure canonical coverage and publish 04-10 truth evidence</name>
  <files>.planning/phases/04-coverage-integrity-enforcement/04-10-COVERAGE.md</files>
  <action>Run canonical coverage commands, capture workspace totals, and compare deltas against 04-09. Record module snapshots for `spreadsheet.rs`, `inline.rs`, `worksheet.rs`, `presentation_helpers.rs`, and `helpers.rs`. Treat completion acceptance as blocked unless the canonical fail-under command exits 0.</action>
  <verify>
    <automated>bash scripts/tests/quality_gate_coverage_commands.sh</automated>
    <automated>cargo llvm-cov --workspace --all-features --summary-only --fail-under-lines 95</automated>
    <automated>cargo llvm-cov --workspace --all-features --summary-only</automated>
  </verify>
  <done>04-10 coverage evidence captures canonical acceptance truth and demonstrates `cargo llvm-cov --workspace --all-features --summary-only --fail-under-lines 95` exits 0.</done>
</task>
</tasks>

<verification>
- [ ] `cargo test -p docir-parser odf::spreadsheet --all-features` passes with behavior assertions for malformed and fallback scenarios.
- [ ] `cargo test -p docir-parser ooxml::docx::document::inline --all-features` passes with explicit inline parse/fallback outcome assertions.
- [ ] `cargo test -p docir-parser ooxml::xlsx::worksheet --all-features` passes with relationship and fallback behavior coverage.
- [ ] `cargo test -p docir-parser odf::presentation_helpers --all-features` passes with edge-case classification assertions.
- [ ] `cargo test -p docir-parser odf::helpers --all-features` passes with non-trivial helper parsing/coercion behavior checks.
- [ ] `bash scripts/tests/quality_gate_coverage_commands.sh` passes.
 - [ ] `cargo llvm-cov --workspace --all-features --summary-only --fail-under-lines 95` exits 0; this is the explicit completion acceptance contract for 04-10.
</verification>

<success_criteria>
- 04-10 increases coverage through behavior-first assertions in the highest-missed residual modules from 04-09.
- Canonical coverage command semantics remain unchanged and authoritative for CC-04 (`--fail-under-lines 95`).
- 04-10 completion acceptance is granted only when `cargo llvm-cov --workspace --all-features --summary-only --fail-under-lines 95` exits 0.
</success_criteria>

<output>
After completion, create `.planning/phases/04-coverage-integrity-enforcement/04-10-SUMMARY.md`
</output>
