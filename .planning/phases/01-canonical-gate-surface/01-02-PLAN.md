---
phase: 01-canonical-gate-surface
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - scripts/quality_gate.sh
  - scripts/tests/quality_gate_exit_codes.sh
  - logs/quality-gate/.gitkeep
autonomous: true
requirements:
  - GATE-02
user_setup: []
must_haves:
  truths:
    - "Canonical gate returns exit code 0 only when all required stages pass."
    - "Canonical gate returns non-zero on both quality-check failures and invocation/precondition failures."
    - "Failure class is observable in terminal output and final machine-parsable status line."
  artifacts:
    - "scripts/tests/quality_gate_exit_codes.sh validates pass, quality-fail, and precondition-fail behavior."
    - "logs/quality-gate/ can store deterministic run evidence without changing exit semantics."
  key_links:
    - "scripts/tests/quality_gate_exit_codes.sh invokes ./scripts/quality_gate.sh directly for every scenario."
---

<objective>
Implement deterministic exit behavior and executable verification for pass/fail/precondition outcomes.

Purpose: Make exit semantics unambiguous for local automation and future CI required checks.
Output: Exit code mapping implementation in canonical script plus scenario-based contract tests and evidence location.
</objective>

<execution_context>
@/Users/seifreed/.codex/get-shit-done/workflows/execute-plan.md
@/Users/seifreed/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-canonical-gate-surface/01-RESEARCH.md
@.planning/phases/01-canonical-gate-surface/01-01-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement strict exit code classification in canonical gate</name>
  <files>scripts/quality_gate.sh</files>
  <action>Wire canonical stage runner to classify outcomes into `0` (full pass), `1` (quality check failure), and `2` (invocation/precondition failure). Ensure classification is deterministic and reflected in final `QUALITY_GATE_RESULT` output metadata.</action>
  <verify>rg "exit 0|exit 1|exit 2|QUALITY_GATE_RESULT=" scripts/quality_gate.sh</verify>
  <done>Canonical script deterministically emits required status and exact exit code classes with no ambiguous fallthrough behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add scenario tests for pass/fail/precondition behavior</name>
  <files>scripts/tests/quality_gate_exit_codes.sh</files>
  <action>Create test scenarios that intentionally trigger each exit path and assert both shell exit code and terminal status marker. Keep all scenarios black-box by calling `./scripts/quality_gate.sh` as end users and CI will.</action>
  <verify>bash scripts/tests/quality_gate_exit_codes.sh</verify>
  <done>Tests fail if any scenario returns wrong code or missing final status line.</done>
</task>

<task type="auto">
  <name>Task 3: Add deterministic evidence directory convention</name>
  <files>logs/quality-gate/.gitkeep</files>
  <action>Introduce stable log destination for captured gate run artifacts used during execution summaries. Keep directory passive so it does not affect gate behavior or introduce alternate invocation path.</action>
  <verify>test -f logs/quality-gate/.gitkeep</verify>
  <done>Evidence directory exists and is ready for reproducible run-output capture.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `bash scripts/tests/quality_gate_exit_codes.sh` passes
- [ ] Manual pass run of `./scripts/quality_gate.sh` returns `0`
- [ ] Forced check failure run returns `1`
- [ ] Forced precondition failure run returns `2`
</verification>

<success_criteria>
- All tasks completed
- Exit code policy is deterministic and test-backed
- Final status output clearly distinguishes pass/fail/precondition classes
- No alternate execution path is introduced
</success_criteria>

<output>
After completion, create `.planning/phases/01-canonical-gate-surface/01-02-SUMMARY.md`
</output>
