---
phase: 01-canonical-gate-surface
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/quality_gate.sh
  - scripts/lib/quality_gate_lib.sh
  - scripts/tests/quality_gate_contract.sh
autonomous: true
requirements:
  - GATE-01
user_setup: []
must_haves:
  truths:
    - "Repository exposes exactly one canonical quality gate entrypoint at ./scripts/quality_gate.sh."
    - "Canonical script can be executed directly from repository root in a deterministic stage order."
  artifacts:
    - "scripts/quality_gate.sh exists, is executable, and contains strict shell mode with deterministic stage runner."
    - "scripts/lib/quality_gate_lib.sh centralizes shared gate functions so no second entrypoint is introduced."
  key_links:
    - "scripts/quality_gate.sh sources scripts/lib/quality_gate_lib.sh and does not defer to any alternate gate wrapper."
---

<objective>
Create the canonical quality gate entrypoint and contract-preserving shell structure.

Purpose: Establish the single accepted gate surface early so every later phase extends one stable command.
Output: Executable canonical gate script, shared internal helper library, and initial contract test script.
</objective>

<execution_context>
@/Users/seifreed/.codex/get-shit-done/workflows/execute-plan.md
@/Users/seifreed/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-canonical-gate-surface/01-RESEARCH.md
@README.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add canonical gate script with deterministic contract scaffolding</name>
  <files>scripts/quality_gate.sh</files>
  <action>Create the canonical entrypoint script at the exact required path with `set -euo pipefail`, deterministic stage ordering, stable logging prefixes, and one final machine-parsable terminal status line. Implement stage dispatch through internal helper functions only, never through alternate external scripts.</action>
  <verify>test -x scripts/quality_gate.sh && rg "set -euo pipefail|QUALITY_GATE_RESULT=" scripts/quality_gate.sh</verify>
  <done>Script exists, is executable, and contains deterministic execution scaffolding plus explicit final status output.</done>
</task>

<task type="auto">
  <name>Task 2: Add internal helper library used only by canonical script</name>
  <files>scripts/lib/quality_gate_lib.sh</files>
  <action>Implement reusable functions for stage logging, command execution wrappers, and status normalization in an internal library that is not a runnable gate entrypoint. Ensure helper file cannot be confused with a gate command surface.</action>
  <verify>rg "run_stage|emit_result|classify_failure" scripts/lib/quality_gate_lib.sh</verify>
  <done>Helper module exists with reusable primitives; canonical script sources it directly and no additional user-facing gate command appears.</done>
</task>

<task type="auto">
  <name>Task 3: Add contract smoke test for canonical path uniqueness</name>
  <files>scripts/tests/quality_gate_contract.sh</files>
  <action>Create a lightweight shell test that validates canonical script presence/executability and scans `scripts/` for alternate gate-like executables that could violate the single-entrypoint policy.</action>
  <verify>bash scripts/tests/quality_gate_contract.sh</verify>
  <done>Contract smoke test passes and fails when alternate gate-like scripts are introduced.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `bash scripts/tests/quality_gate_contract.sh` passes
- [ ] `./scripts/quality_gate.sh --help` (or equivalent no-op mode) exits successfully
- [ ] `find scripts -maxdepth 2 -type f | rg "gate|quality|check"` shows no second executable gate entrypoint
</verification>

<success_criteria>
- All tasks completed
- Canonical entrypoint exists only at `./scripts/quality_gate.sh`
- Internal helpers are non-entrypoint support code only
- No warnings or errors introduced by new shell scripts
</success_criteria>

<output>
After completion, create `.planning/phases/01-canonical-gate-surface/01-01-SUMMARY.md`
</output>
