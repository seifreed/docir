---
phase: 03-baseline-clean-code-commands
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - scripts/tests/quality_gate_exit_codes.sh
  - scripts/tests/quality_gate_baseline_commands.sh
  - README.md
  - docs/quality-gate-policy.md
autonomous: true
requirements:
  - TEST-03
must_haves:
  truths:
    - "Tests prove canonical gate executes baseline cargo commands with strict warning posture and no suppression bypass."
    - "Failure in any baseline command path is observable as canonical quality failure with deterministic exit semantics."
    - "Policy/docs state raw cargo commands are diagnostic-only while canonical gate remains acceptance authority."
  artifacts:
    - "scripts/tests/quality_gate_baseline_commands.sh validates command order/arguments and stage fail behavior via deterministic test harness."
    - "scripts/tests/quality_gate_exit_codes.sh covers pass/quality-fail/precondition-fail contract with baseline-enabled gate."
    - "README.md and docs/quality-gate-policy.md document no-suppression posture and canonical-only acceptance semantics."
  key_links:
    - "Test scripts execute ./scripts/quality_gate.sh and assert final QUALITY_GATE_RESULT classification."
    - "Documentation links acceptance behavior to canonical gate script and policy source without alternate authoritative paths."
---

<objective>
Add deterministic evidence and policy alignment for warning-strict baseline enforcement with no suppression acceptance path.

Purpose: Convert TEST-03 from implicit intent into repeatable proof and explicit repository policy.
Output: Black-box test coverage for baseline command strictness plus aligned documentation wording that forbids warning suppression as a pass strategy.
</objective>

<execution_context>
@/Users/seifreed/.codex/get-shit-done/workflows/execute-plan.md
@/Users/seifreed/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-baseline-clean-code-commands/03-CONTEXT.md
@.planning/phases/03-baseline-clean-code-commands/03-RESEARCH.md
@.planning/phases/03-baseline-clean-code-commands/03-01-SUMMARY.md
@scripts/tests/quality_gate_exit_codes.sh
@README.md
@docs/quality-gate-policy.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add deterministic baseline-command contract test harness</name>
  <files>scripts/tests/quality_gate_baseline_commands.sh</files>
  <action>Create a black-box test script that validates canonical gate command sequence and strict arguments using a temporary cargo shim (`PATH` override). Assert exact baseline invocations (`fmt --all --check`, `clippy --all-targets --all-features -- -D warnings`, `test`) and locked order. Include deterministic simulated failures per stage and assert canonical final line class/exit outcomes remain quality failures.</action>
  <verify>
    <automated>bash scripts/tests/quality_gate_baseline_commands.sh</automated>
  </verify>
  <done>Repository has deterministic tests proving baseline command enforcement and fail-fast behavior through canonical execution only.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate baseline harness into existing gate exit contract checks</name>
  <files>scripts/tests/quality_gate_exit_codes.sh</files>
  <action>Update exit-code contract script to include the new baseline-command harness in its verification flow or explicitly invoke both scripts in a deterministic sequence. Preserve existing pass/quality-fail/precondition-fail assertions and final `quality_gate_exit_codes: OK` signal format.</action>
  <verify>
    <automated>bash scripts/tests/quality_gate_exit_codes.sh</automated>
  </verify>
  <done>Exit contract tests now cover both baseline-stage behavior and canonical final line semantics in one reproducible CI/local command.</done>
</task>

<task type="auto">
  <name>Task 3: Align docs with strict warning posture and non-suppression policy</name>
  <files>README.md, docs/quality-gate-policy.md</files>
  <action>Document that clippy warnings are denied in canonical runs and no suppression flags/config (`-A`, env bypasses, or acceptance-time allows) are valid for gate passing. Keep direct cargo command references as diagnostic-only guidance and preserve canonical-only acceptance wording.</action>
  <verify>
    <automated>rg -n "clippy.*-D warnings|suppression|-A|diagnostic-only|canonical" README.md docs/quality-gate-policy.md</automated>
  </verify>
  <done>Documentation explicitly matches TEST-03 posture and remains consistent with canonical-only enforcement model.</done>
</task>
</tasks>

<verification>
- [ ] `bash scripts/tests/quality_gate_contract.sh` passes.
- [ ] `bash scripts/tests/quality_gate_exit_codes.sh` passes and includes baseline command strictness assertions.
- [ ] `bash scripts/tests/quality_gate_baseline_commands.sh` passes as standalone proof harness.
- [ ] `rg -n "clippy.*-D warnings|suppression|-A|diagnostic-only|canonical" README.md docs/quality-gate-policy.md` confirms aligned policy wording.
</verification>

<success_criteria>
- TEST-03 has deterministic black-box verification and explicit policy coverage.
- Baseline stage behavior is verifiable without adding alternate acceptance scripts.
- Canonical-only acceptance contract remains intact across tests and docs.
</success_criteria>

<output>
After completion, create `.planning/phases/03-baseline-clean-code-commands/03-02-SUMMARY.md`
</output>
