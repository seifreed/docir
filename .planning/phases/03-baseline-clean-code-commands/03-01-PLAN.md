---
phase: 03-baseline-clean-code-commands
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/quality_gate.sh
  - scripts/lib/quality_gate_lib.sh
autonomous: true
requirements:
  - CC-01
  - CC-02
  - CC-03
must_haves:
  truths:
    - "Canonical gate runs baseline checks in locked order: fmt check, clippy warnings denied, then tests."
    - "Any failing baseline stage ends execution and returns quality failure class."
    - "Canonical final status line remains machine-parseable and deterministic."
  artifacts:
    - "scripts/quality_gate.sh defines explicit baseline stages for fmt, clippy, and test commands in the dispatcher loop."
    - "scripts/lib/quality_gate_lib.sh provides shared helpers needed for deterministic stage execution without changing exit-class contract."
  key_links:
    - "run_default_stages in scripts/quality_gate.sh executes stage functions through run_stage and classify_failure."
    - "emit_final_result in scripts/quality_gate.sh maps classified exit code to QUALITY_GATE_RESULT final line."
---

<objective>
Replace scaffold-only quality stage execution with real baseline clean-code command enforcement in the canonical gate.

Purpose: Satisfy mandatory baseline quality checks in the single accepted gate path without creating alternate orchestration surfaces.
Output: Canonical gate script and shared library logic that execute `cargo fmt --all --check`, `cargo clippy --all-targets --all-features -- -D warnings`, and `cargo test` in locked order with strict failure propagation.
</objective>

<execution_context>
@/Users/seifreed/.codex/get-shit-done/workflows/execute-plan.md
@/Users/seifreed/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-baseline-clean-code-commands/03-CONTEXT.md
@.planning/phases/03-baseline-clean-code-commands/03-RESEARCH.md
@scripts/quality_gate.sh
@scripts/lib/quality_gate_lib.sh
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add explicit baseline stage functions and locked-stage pipeline</name>
  <files>scripts/quality_gate.sh</files>
  <action>Replace the placeholder `contract_scaffold` stage with explicit stage handlers for formatting, clippy warnings-as-errors, and test execution. Keep locked stage order exactly `fmt_check` -> `clippy_strict` -> `test_workspace` after precondition/tooling checks. Use exact commands required by phase decisions: `cargo fmt --all --check`, `cargo clippy --all-targets --all-features -- -D warnings`, and `cargo test`.</action>
  <verify>
    <automated>rg -n "fmt.*--all --check|clippy --all-targets --all-features -- -D warnings|cargo test|for stage in .*fmt.*clippy.*test" scripts/quality_gate.sh</automated>
  </verify>
  <done>Canonical gate stage list contains real baseline command stages in the locked order and no scaffold acceptance stage.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve deterministic failure-class behavior and stage logging contracts</name>
  <files>scripts/quality_gate.sh, scripts/lib/quality_gate_lib.sh</files>
  <action>Ensure baseline stage failures continue to flow through `run_stage` and `classify_failure` without suppression or exit-code rewrites. Preserve stage start/pass/fail log format and final `QUALITY_GATE_RESULT=... CLASS=... EXIT_CODE=...` line contract. Keep precondition failures mapped to class `precondition_failure` and baseline command failures mapped to `quality_failure`.</action>
  <verify>
    <automated>bash scripts/tests/quality_gate_exit_codes.sh</automated>
  </verify>
  <done>Exit semantics and machine-parseable final status line remain stable while baseline commands are now authoritative checks.</done>
</task>

<task type="auto">
  <name>Task 3: Execute canonical gate once to confirm end-to-end baseline invocation path</name>
  <files>scripts/quality_gate.sh</files>
  <action>Run `./scripts/quality_gate.sh` from repository root and confirm the run executes baseline stages and exits with deterministic status. Do not add or use any alternate acceptance wrapper command.</action>
  <verify>
    <automated>./scripts/quality_gate.sh</automated>
  </verify>
  <done>A full canonical run proves baseline stage path is active in the only accepted gate entrypoint.</done>
</task>
</tasks>

<verification>
- [ ] `bash scripts/tests/quality_gate_contract.sh` passes, proving canonical gate remains sole accepted entrypoint.
- [ ] `bash scripts/tests/quality_gate_exit_codes.sh` passes with unchanged final result contract semantics.
- [ ] `./scripts/quality_gate.sh` runs baseline checks in locked order and exits deterministically.
</verification>

<success_criteria>
- CC-01, CC-02, and CC-03 are implemented directly in the canonical gate stage sequence.
- Baseline command failures cannot bypass deterministic quality-failure classification.
- No alternate acceptance path is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/03-baseline-clean-code-commands/03-01-SUMMARY.md`
</output>
